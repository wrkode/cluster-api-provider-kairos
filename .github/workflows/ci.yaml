name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GOTOOLCHAIN: local
    strategy:
      fail-fast: false
      matrix:
        go-version: ['1.26']  # go.mod requires go 1.26
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.26.4'

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-1.26.0-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-1.26.0-

    - name: Download dependencies
      run: go mod download

    - name: Run go vet
      run: go vet ./...

    - name: Run tests
      run: go test ./... -v -short -coverprofile=coverage.out

    - name: Upload coverage
      uses: codecov/codecov-action@v4
      if: always()
      with:
        file: ./coverage.out
        flags: unittests
        name: codecov-umbrella

  verify-generate:
    runs-on: ubuntu-latest
    env:
      GOTOOLCHAIN: local
    strategy:
      fail-fast: false
      matrix:
        go-version: ['1.26']  # go.mod requires go 1.26
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.26.4'

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-1.26.0-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-1.26.0-

    - name: Install controller-gen
      run: |
        go install sigs.k8s.io/controller-tools/cmd/controller-gen@v0.17.0

    - name: Generate code
      run: make generate

    - name: Verify generated code
      run: |
        git diff --exit-code || (echo "Error: Generated code is out of date. Run 'make generate' and commit the changes." && exit 1)

  verify-manifests:
    runs-on: ubuntu-latest
    env:
      GOTOOLCHAIN: local
    strategy:
      fail-fast: false
      matrix:
        go-version: ['1.26']  # go.mod requires go 1.26
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.26.4'

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-1.26.0-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-1.26.0-

    - name: Install controller-gen
      run: |
        go install sigs.k8s.io/controller-tools/cmd/controller-gen@v0.17.0

    - name: Install ruamel.yaml (for label insertion with formatting preservation)
      run: |
        python3 -m pip install --user ruamel.yaml

    - name: Generate manifests
      run: make manifests

    - name: Verify manifests
      run: |
        git diff --exit-code config/crd/bases config/rbac config/webhook || (echo "Error: Manifests are out of date. Run 'make manifests' and commit the changes." && exit 1)

  test-envtest:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go-version: ['1.26']  # go.mod requires go 1.26
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.26.4'

    - name: Install envtest tools
      run: |
        go install sigs.k8s.io/controller-runtime/tools/setup-envtest@latest
        export PATH=$(go env GOPATH)/bin:$PATH
        setup-envtest use -p path latest

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-1.26.0-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-1.26.0-

    - name: Download dependencies
      run: go mod download

    - name: Run envtest integration tests
      run: make test-envtest
      env:
        KUBEBUILDER_ASSETS: $(setup-envtest use -p path latest)

  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 140
    env:
      GOTOOLCHAIN: local
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.26.4'

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-1.26.0-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-1.26.0-

    - name: Cache pinned e2e CLIs (kubectl, kind, clusterctl, virtctl)
      uses: actions/cache@v4
      with:
        path: .e2e-bin
        key: ${{ runner.os }}-e2e-bin-${{ hashFiles('internal/kubevirtenv/binaries.go') }}

    - name: Free up disk space
      run: |
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL || true
        df -h

    - name: Run e2e suite
      # Use `go run .../ginkgo` so the CLI version always matches the Ginkgo library in go.mod.
      run: go run github.com/onsi/ginkgo/v2/ginkgo -v --timeout=130m ./test/e2e/...

  build:
    runs-on: ubuntu-latest
    needs: [test, verify-generate, verify-manifests]
    env:
      GOTOOLCHAIN: local
    strategy:
      fail-fast: false
      matrix:
        go-version: ['1.26']  # go.mod requires go 1.26
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.26.4'

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-1.26.0-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-1.26.0-

    - name: Download dependencies
      run: go mod download

    - name: Build
      run: make build

